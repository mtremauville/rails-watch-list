<div class="container">
  <%= simple_form_for([@list, @bookmark]) do |f| %>
    <%= f.association :movie %>
    <%= f.input :comment %>
    <%= f.button :submit, class:"btn-success text-white" %>
  <% end %>
</div>
