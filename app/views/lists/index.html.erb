<h2>My lists</h2>
<ul class="list-unstyled ps-0">
  <% @lists.each do |list| %>
    <li ><%= link_to list.name, list_path(list) %>
        <%= link_to "Supprimer", list_path(list), data: { turbo_method: :delete, turbo_confirm: "Supprimer cette liste ?" } %>
    </li>
  <% end %>
</ul>
